const config = {
  staticUrl: 'http://localhost:5000', // url of static server
  socketUrl: 'https://127.0.0.1:5001', // url of socket server
  idleTimeThreshold: 600, // seconds of inactivity considering user is idle
  activityInterval: 60,
};

export default config;
